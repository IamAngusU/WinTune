const search=document.querySelector('#search');const filters=[...document.querySelectorAll('[data-filter]')];const entries=[...document.querySelectorAll('[data-search]')];const update=()=>{const query=search.value.toLowerCase(),filter=document.querySelector('nav .active').dataset.filter;entries.forEach(entry=>entry.hidden=!(entry.dataset.search.includes(query)&&(filter==='all'||entry.dataset.tags.includes(filter))));};search.addEventListener('input',update);filters.forEach(button=>button.addEventListener('click',()=>{filters.forEach(item=>item.classList.remove('active'));button.classList.add('active');update();}));document.querySelectorAll('[data-copy]').forEach(button=>button.addEventListener('click',async()=>{await navigator.clipboard.writeText(button.dataset.copy);button.textContent='Copied';setTimeout(()=>button.textContent='Copy SHA-256',1400);}));
