const input=document.querySelector('#search');const buttons=[...document.querySelectorAll('[data-filter]')];const releases=[...document.querySelectorAll('.release')];const update=()=>{const query=input.value.toLowerCase();const filter=document.querySelector('.filters .selected').dataset.filter;releases.forEach(item=>{item.hidden=!(item.dataset.search.includes(query)&&(filter==='all'||item.dataset.tags.includes(filter)));});};input.addEventListener('input',update);buttons.forEach(button=>button.addEventListener('click',()=>{buttons.forEach(item=>item.classList.remove('selected'));button.classList.add('selected');update();}));document.querySelectorAll('[data-copy]').forEach(button=>button.addEventListener('click',async()=>{await navigator.clipboard.writeText(button.dataset.copy);button.textContent='Copied';setTimeout(()=>button.textContent='Copy SHA-256',1400);}));
